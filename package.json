{
  "name": "ultimaitron-ai",
  "private": true,
  "version": "1.0.0",
  "description": "Ultimaitron AI — a webcam- and gesture-controlled holographic neural AI core built with Three.js and MediaPipe.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.22",
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
